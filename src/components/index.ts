import type { ComponentProps } from 'react';
import type { Size } from './types';

// If you have a correct source for Variant, import it here, otherwise remove it from export
export type { 
  ComponentProps, 
  Size
};